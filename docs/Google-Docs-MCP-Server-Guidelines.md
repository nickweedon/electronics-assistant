# **Google Docs MCP Server Guidelines**

# **Google Docs MCP Server Guidelines**

Follow these recommendations when using the Google Docs MCP Server

* NEVER write documentation in markdown. The Google Docs MCP server lacks the capability to translate markdown and so this will not render.
* Always favor using bulk operations when doing so can minimize the number of MCP calls to the server.
